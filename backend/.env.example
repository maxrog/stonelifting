# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=your_db_username
DATABASE_PASSWORD=your_db_password
DATABASE_NAME=your_db_name

# Security Configuration
# IMPORTANT: Generate a strong random secret (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_STRONG_SECRET_MINIMUM_32_CHARS

# CORS Configuration
# For development: http://localhost:3000 (default if not set)
# For production: Set to your actual domain (e.g., https://stonelifting.app)
CORS_ALLOWED_ORIGIN=http://localhost:3000
